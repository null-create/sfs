package pkg

/*
manager will need the folloing functionalities

- acts a server listener (wrapping the http standard lib server
defined in server.server.go) and handling incoming requests

- intialize handlers and attach to server struct

- initializes the base service struct and attaches it to the server struct

- handle sync events (file uploads/downloadss)

- create and handle user events
*/
