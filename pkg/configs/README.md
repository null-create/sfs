# About

Component for exporting application configurations defined in a YAML file to
the env package.

Basically a wrapper for the env component so we don't need to have .env files wherever we call 'sfs' from the command line.